# Module Status: `frontend_app`

## 基本信息

- `module`: `frontend_app`
- `status`: `building`
- `branch`: `module/frontend_app`
- `owner`: `codex`
- `updated_at`: `2026-03-31`

## 模块目标

提供图片上传、会话练习、反馈展示和历史查看的前端壳层与交互体验。

## 当前真相

- 现有实现：`frontend/` 已补 React + Tailwind + Vitest 工程骨架，并新增首页与首批组件文件
- 已完成边界：设计工作流文档、Pencil 设计源、设计映射文档、前端工程初始化
- 仍然缺失：以 `.pen` 稿为准的前端校正、真实接口调用、前端验证收口

## 输入输出契约

- 输入：图片文件、会话数据、反馈数据、历史数据
- 输出：页面状态、交互反馈、可视化展示
- 关键字段：上传状态、消息列表、反馈卡片、历史列表

## 代码位置

- `frontend/`

## 当前工作

- `[x]` 初始化可运行前端工程
- `[x]` 对齐后端 API 契约的设计语义和 mock 数据结构
- `[x]` 定义上传页、练习页、历史页最小壳层
- `[ ]` 按 Pencil 设计稿完成前端代码校正
- `[ ]` 形成可复述的前端构建与测试通过证据

## 测试门

- `module_test_passed` 的标准：至少有最小可运行前端壳层并能对齐后端契约，且前端构建与测试有明确通过证据
- 最少要覆盖的用例：上传入口、会话展示、历史入口
- 还没覆盖的风险：当前真实 API 尚未接入，页面代码也还未完全按 Pencil 设计稿回贴

## 阻塞项

- 前端测试和构建尚未形成最终通过证据
- 后端 API 路由还未接通前端主流程

## 变更记录

- `2026-03-30`：初始化模块状态文档
- `2026-03-31`：补齐 Pencil-first 工作流文档，生成 MVP `.pen` 原型文件，并初始化前端工程骨架
